const test: number | string = '3';
let validation: boolean = true;
let total: number = 0;
let ecole: string = 'webforce3';
let table: Array<string | number> = [1, 2, 3, 'test'];
let notes: {prenom: string, note: number} = {prenom: 'prenom', note: 0};
// let maVariable: any;
// maVariable = 'test';
// maVariable.toUppercase();
